[package]
name = "openjarvis-cross-device-travel"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false

# An isolated handoff example: no changes to the OpenJarvis runtime or workspace.
[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
